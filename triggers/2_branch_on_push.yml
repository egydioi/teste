name: GitHub Actions - Push - Branch Template
run-name: Running ${{github.event_name}}
on: 
    push:
        branches: 
            - develop
            - master
            - 'release/**'
jobs:
    on-push:
        runs-on: ubuntu-latest
        steps:
            - name: Comentário de teste
              run: |
                echo "Olá, este é um teste de push para a branch develop!"